// Reuse the institutional travel agents page for owners
// The component uses useAuth to get the user's organization_id,
// so it works for any role that has an organization with travel agents assigned.
export { default } from '@/pages/institutional/InstitutionalTravelAgents';
